package rotp.ui;

import java.awt.BasicStroke;
import java.awt.Stroke;

public interface ScaledInteger {
	int s1	= RotPUI.scaledSize(1);
	int s2	= RotPUI.scaledSize(2);
	int s3	= RotPUI.scaledSize(3);
	int s4	= RotPUI.scaledSize(4);
	int s5	= RotPUI.scaledSize(5);
	int s6	= RotPUI.scaledSize(6);
	int s7	= RotPUI.scaledSize(7);
	int s8	= RotPUI.scaledSize(8);
	int s9	= RotPUI.scaledSize(9);
	int s10	= RotPUI.scaledSize(10);
	int s11	= RotPUI.scaledSize(11);
	int s12	= RotPUI.scaledSize(12);
	int s13	= RotPUI.scaledSize(13);
	int s14	= RotPUI.scaledSize(14);
	int s15	= RotPUI.scaledSize(15);
	int s16	= RotPUI.scaledSize(16);
	int s17	= RotPUI.scaledSize(17);
	int s18	= RotPUI.scaledSize(18);
	int s19	= RotPUI.scaledSize(19);
	int s20	= RotPUI.scaledSize(20);
	int s21	= RotPUI.scaledSize(21);
	int s22	= RotPUI.scaledSize(22);
	int s23	= RotPUI.scaledSize(23);
	int s24	= RotPUI.scaledSize(24);
	int s25	= RotPUI.scaledSize(25);
	int s26	= RotPUI.scaledSize(26);
	int s27	= RotPUI.scaledSize(27);
	int s28	= RotPUI.scaledSize(28);
	int s29	= RotPUI.scaledSize(29);
	int s30	= RotPUI.scaledSize(30);
	int s31	= RotPUI.scaledSize(31);
	int s32	= RotPUI.scaledSize(32);
	int s33	= RotPUI.scaledSize(33);
	int s34	= RotPUI.scaledSize(34);
	int s35	= RotPUI.scaledSize(35);
	int s36	= RotPUI.scaledSize(36);
	int s37	= RotPUI.scaledSize(37);
	int s38	= RotPUI.scaledSize(38);
	int s39	= RotPUI.scaledSize(39);
	int s40	= RotPUI.scaledSize(40);
	int s41	= RotPUI.scaledSize(41);
	int s42	= RotPUI.scaledSize(42);
	int s43	= RotPUI.scaledSize(43);
	int s44	= RotPUI.scaledSize(44);
	int s45	= RotPUI.scaledSize(45);
	int s46	= RotPUI.scaledSize(46);
	int s47	= RotPUI.scaledSize(47);
	int s48	= RotPUI.scaledSize(48);
	int s49	= RotPUI.scaledSize(49);
	int s50	= RotPUI.scaledSize(50);
	int s51	= RotPUI.scaledSize(51);
	int s52	= RotPUI.scaledSize(52);
	int s53	= RotPUI.scaledSize(53);
	int s54	= RotPUI.scaledSize(54);
	int s55	= RotPUI.scaledSize(55);
	int s56	= RotPUI.scaledSize(56);
	int s57	= RotPUI.scaledSize(57);
	int s58	= RotPUI.scaledSize(58);
	int s59	= RotPUI.scaledSize(59);
	int s60	= RotPUI.scaledSize(60);
	int s61	= RotPUI.scaledSize(61);
	int s62	= RotPUI.scaledSize(62);
	int s63	= RotPUI.scaledSize(63);
	int s64	= RotPUI.scaledSize(64);
	int s65	= RotPUI.scaledSize(65);
	int s66	= RotPUI.scaledSize(66);
	int s67	= RotPUI.scaledSize(67);
	int s68	= RotPUI.scaledSize(68);
	int s69	= RotPUI.scaledSize(69);
	int s70	= RotPUI.scaledSize(70);
	int s71	= RotPUI.scaledSize(71);
	int s72	= RotPUI.scaledSize(72);
	int s73	= RotPUI.scaledSize(73);
	int s74	= RotPUI.scaledSize(74);
	int s75	= RotPUI.scaledSize(75);
	int s76	= RotPUI.scaledSize(76);
	int s77	= RotPUI.scaledSize(77);
	int s78	= RotPUI.scaledSize(78);
	int s79	= RotPUI.scaledSize(79);
	int s80	= RotPUI.scaledSize(80);
	int s81	= RotPUI.scaledSize(81);
	int s82	= RotPUI.scaledSize(82);
	int s83	= RotPUI.scaledSize(83);
	int s84	= RotPUI.scaledSize(84);
	int s85	= RotPUI.scaledSize(85);
	int s86	= RotPUI.scaledSize(86);
	int s87	= RotPUI.scaledSize(87);
	int s88	= RotPUI.scaledSize(88);
	int s89	= RotPUI.scaledSize(89);
	int s90	= RotPUI.scaledSize(90);
	int s91	= RotPUI.scaledSize(91);
	int s92	= RotPUI.scaledSize(92);
	int s93	= RotPUI.scaledSize(93);
	int s94	= RotPUI.scaledSize(94);
	int s95	= RotPUI.scaledSize(95);
	int s96	= RotPUI.scaledSize(96);
	int s97	= RotPUI.scaledSize(97);
	int s98	= RotPUI.scaledSize(98);
	int s99	= RotPUI.scaledSize(99);
	int s100 = RotPUI.scaledSize(100);
	int s200 = RotPUI.scaledSize(200);

	Stroke stroke1 = new BasicStroke(s1, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: round cap and join
	Stroke stroke2 = new BasicStroke(s2, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: round cap and join
	Stroke stroke3 = new BasicStroke(s3, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: round cap and join
	Stroke stroke4 = new BasicStroke(s4, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: round cap and join
	Stroke stroke5 = new BasicStroke(s5, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: round cap and join
	Stroke stroke6 = new BasicStroke(s6, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: round cap and join
	Stroke stroke7 = new BasicStroke(s7, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: round cap and join
	Stroke stroke8 = new BasicStroke(s8, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: add strokes
	Stroke stroke9 = new BasicStroke(s9, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: add strokes
	Stroke stroke10 = new BasicStroke(s10, BasicStroke.CAP_ROUND, BasicStroke.JOIN_ROUND); // modnar: add strokes
	default Stroke baseStroke(int n) {
		switch(n) {
			case 1: return stroke1;
			case 2: return stroke2;
			case 3: return stroke3;
			case 4: return stroke4;
			case 5: return stroke5;
			case 6: return stroke6;
			case 7: return stroke7;
			case 8: return stroke8; // modnar: add strokes
			case 9: return stroke9; // modnar: add strokes
			case 10: return stroke10; // modnar: add strokes
			default: return stroke1;
			}
		}
	int cnr = s5; // corner radius
}
